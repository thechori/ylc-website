// PostController.js
// var $ = require('jquery'); // i think this is already included on the main template page
var mongoose = require('mongoose');

module.exports = {

  hello: function() {
    console.log('hello');
  }
  
};
